

// Ici, c'est la définition du type de variable contenues dans le tableau appPosts.
export interface typePost {
  titlePost: string;
  contentPost: string;
  loveIts: number;
  created_at: Date
} ;